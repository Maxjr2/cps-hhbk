{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"CPS Project documentation","text":"<p>This documentation contains:</p> <ul> <li>Getting Started instructions  </li> <li>A wiki for all Actor modules  </li> <li>A wiki for all Sensor modules  </li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<p>Clone the repository:</p> <pre><code>git clone https://github.com/Maxjr2/cps-hhbk.git\ncd cps-hhbk\n</code></pre>"},{"location":"sensors/sensor-KY001/","title":"KY-001 Temperatursensor (DS18B20)","text":""},{"location":"sensors/sensor-KY001/#ky-001-temperature-sensor-ds18b20","title":"KY-001 Temperature Sensor (DS18B20)","text":""},{"location":"sensors/sensor-KY001/#overview","title":"Overview","text":"<p>This page documents the <code>sensors/ky001.py</code> Python module which reads temperature data from a KY-001 (DS18B20) 1-Wire temperature sensor and explains how to integrate it into <code>main.py</code>.</p>"},{"location":"sensors/sensor-KY001/#requirements","title":"Requirements","text":"<ul> <li>The Linux kernel must have the 1-Wire drivers loaded (for example <code>w1-gpio</code> and <code>w1-therm</code>).</li> <li>The sensor device is exposed under <code>/sys/bus/w1/devices/</code>. Devices typically appear in a directory starting with <code>28-</code>.</li> <li>The module reads the <code>w1_slave</code> file in the device folder. The process needs read permission for that file.</li> </ul>"},{"location":"sensors/sensor-KY001/#wiring","title":"Wiring","text":"<ul> <li>Typical wiring for the KY-001 (DS18B20):<ul> <li>VCC -&gt; 3.3V</li> <li>GND -&gt; GND</li> <li>DATA -&gt; 1-Wire data line (use a ~4.7k\u03a9 pull-up resistor to 3.3V if not provided on your board)</li> </ul> </li> </ul>"},{"location":"sensors/sensor-KY001/#api-brief","title":"API (brief)","text":"<p>The module exposes two functions:</p> <ul> <li> <p><code>read_temp_raw()</code> -&gt; list[str]</p> <ul> <li>Reads the raw <code>w1_slave</code> file and returns its lines as a list of strings.</li> </ul> </li> <li> <p><code>read_temp()</code> -&gt; (temp_c, temp_f) | None</p> <ul> <li>Calls <code>read_temp_raw()</code> until the first line ends with <code>YES</code>, then parses the <code>t=</code> value from the second line and returns a tuple <code>(temp_c, temp_f)</code> where <code>temp_c</code> is Celsius and <code>temp_f</code> is Fahrenheit. If a device is not present the module may raise an error at import time (see notes).</li> </ul> </li> </ul>"},{"location":"sensors/sensor-KY001/#example","title":"Example","text":"<p>Basic usage:</p> <pre><code>from sensors import ky001\n\nresult = ky001.read_temp()\nif result is None:\n        print(\"Temperature could not be read\")\nelse:\n        temp_c, temp_f = result\n        print(f\"Temperature: {temp_c:.2f} \u00b0C / {temp_f:.2f} \u00b0F\")\n</code></pre>"},{"location":"sensors/sensor-KY001/#integration-with-mainpy","title":"Integration with <code>main.py</code>","text":"<p>The repository contains a <code>main.py</code> that imports <code>sensors.ky001</code> and calls <code>read_temp()</code> to decide LED colors. Important notes about integration:</p> <ul> <li>The current <code>sensors.ky001.read_temp()</code> returns a tuple <code>(temp_c, temp_f)</code>. <code>main.py</code> must unpack this tuple and use the Celsius value (or Fahrenheit, if desired) for numeric comparisons.</li> <li>The current <code>main.py</code> code calls <code>temperature = sensors.ky001.read_temp()</code> and then compares <code>temperature &lt; 21</code>. That will raise a TypeError because <code>temperature</code> is a tuple. To fix this, unpack the result and handle the None case.</li> </ul> <p>Recommended, safe pattern for <code>main.py</code>:</p> <pre><code>from sensors import ky001\nimport actors.led\n\ndef get_sensor_state():\n        result = ky001.read_temp()\n        if not result:\n                # handle missing sensor or read error\n                actors.led.set_led(\"yellow\")\n                return\n\n        temp_c, temp_f = result\n        # choose LED color based on Celsius\n        if temp_c &lt; 21:\n                actors.led.set_led(\"green\")\n        elif temp_c &gt;= 31:\n                actors.led.set_led(\"red\")\n        elif temp_c &gt;= 26:\n                actors.led.set_led(\"yellow\")\n\n\nactors.led.selftest()\n</code></pre> <p>Notes on the logic above:</p> <ul> <li>Always check that <code>read_temp()</code> returned a value before unpacking. If <code>read_temp()</code> can block for a long time (waiting for raw data to report <code>YES</code>), consider calling it from a background thread or using a timeout mechanism.</li> <li>The order of range checks matters: check the highest threshold first or use an explicit range mapping.</li> </ul>"},{"location":"sensors/sensor-KY001/#common-issues-and-caveats","title":"Common issues and caveats","text":"<ul> <li> <p>Import-time device lookup: the module currently resolves the device folder using <code>glob.glob(base_dir + '28*')[0]</code> at import time. If no 1-Wire device is present this will raise <code>IndexError</code> during import. Recommended alternatives:</p> <ul> <li>Move device discovery into a helper function that runs on first read.</li> <li>Wrap the initial device lookup in try/except and raise a clearer error.</li> </ul> </li> <li> <p>Blocking reads: <code>read_temp()</code> polls the device file and sleeps (<code>time.sleep(0.2)</code>) until the data is marked valid. On missing or flaky hardware this can block the caller.</p> </li> <li> <p>Permissions: ensure the process user has read access to <code>/sys/bus/w1/devices/.../w1_slave</code>.</p> </li> </ul>"},{"location":"sensors/sensor-KY001/#suggested-module-improvements","title":"Suggested module improvements","text":"<ul> <li>Delay device-folder detection until the first reading.</li> <li>Return a richer result object or raise custom exceptions for error cases instead of returning <code>None</code>.</li> <li>Use <code>with open(...) as f:</code> when reading files to ensure proper resource handling.</li> </ul>"},{"location":"sensors/sensor-KY001/#license","title":"License","text":"<p>The source file contains SPDX license tags indicating the MIT license (see the header in <code>sensors/ky001.py</code>).</p> <p>If you want, I can either update <code>main.py</code> to use <code>read_temp()</code> correctly, or change <code>sensors/ky001.py</code> to return a single Celsius float (or raise clearer exceptions). Which would you prefer me to do next?</p>"}]}